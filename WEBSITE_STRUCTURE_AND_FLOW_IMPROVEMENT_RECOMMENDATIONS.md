# 网站结构与用户流程全面改善建议

生成日期：2026-06-04

适用项目：测试开发面试速成站

当前技术栈：Astro 6 + Starlight + TypeScript + GitHub Pages

## 1. 总体判断

当前站点已经具备较好的内容基础和工程基础：内容量充足、9 个核心模块已经成型、Starlight 文档壳层稳定、搜索和基础索引可用、本地进度和最近浏览已经接入，CI 也覆盖了格式检查、lint、类型检查、内容校验、单元测试、构建和 Chromium 冒烟 E2E。

但从“网站结构”和“用户流程”角度看，当前站点更像一个内容丰富的文档库，还没有完全变成一个有明确学习路径、复习闭环和面试冲刺节奏的产品。主要问题不是内容不够，而是用户在不同目标下进入站点后，下一步该怎么走还不够明确。

核心改善方向：

1. 从“分类导航”升级为“任务导向导航”。
2. 从“内容集合”升级为“学习路径和面试流程”。
3. 从“单篇阅读”升级为“阅读、练习、收藏、复盘、追问”的闭环。
4. 从“首页入口”升级为“回访用户的学习控制台”。
5. 从“内容存在”升级为“内容之间有明确关系和阶段顺序”。

## 2. 当前结构快照

### 2.1 内容分布

当前站点共 123 篇文档，按模块分布如下：

| 模块              | 数量 | 入门级 | 面试级 | 自测题数 | 关联关系数 | 标签数 |
| ----------------- | ---: | -----: | -----: | -------: | ---------: | -----: |
| glossary          |   33 |     17 |     16 |       84 |         81 |    114 |
| tech              |   19 |     11 |      8 |       57 |          0 |     73 |
| project           |   12 |      6 |      6 |       36 |          0 |     45 |
| scenario          |   14 |      2 |     12 |       42 |          0 |     53 |
| coding            |   10 |      3 |      7 |       30 |          0 |     37 |
| roadmap           |    9 |      6 |      3 |       27 |          0 |     33 |
| ai-learning       |    9 |      3 |      6 |       27 |          0 |     33 |
| practice-template |    8 |      5 |      3 |       24 |          0 |     29 |
| interview-chains  |    9 |      0 |      9 |       27 |          0 |     27 |

观察：

- 内容覆盖面很完整，已经不是早期空壳。
- `glossary` 的关联关系较丰富，但其他模块几乎没有 `relatedSlugs`。
- 自测题覆盖较均衡，说明内容具备练习化基础。
- 面试级内容集中在 `scenario`、`coding`、`interview-chains`，适合做冲刺路径。
- 入门级内容集中在 `glossary`、`tech`、`roadmap`、`practice-template`，适合做基础路径。

### 2.2 当前主要入口

当前用户入口主要包括：

- 首页：学习路线、核心模块、学习进度、最近浏览
- Starlight 侧边栏：9 个模块自动导航
- 搜索：Starlight / Pagefind
- 标签页：`/tags/`
- 难度页：`/difficulty/`
- 内容页：自测题、标记完成、收藏、分享、默认分页

### 2.3 当前结构的主要断点

1. 首页模块卡片直接跳单篇推荐内容，缺少模块总览。
2. 非 Starlight 页面缺少完整统一的全站壳层体验。
3. 收藏能力有入口，但缺少收藏列表和回访入口。
4. 最近浏览只出现在首页，内容页没有明显的“继续学习”区域。
5. 大多数内容没有跨模块关联，用户读完一篇后缺少明确下一步。
6. 标签和难度页是静态长列表，不像可筛选的信息检索页。
7. 学习路线与实际内容完成状态尚未强绑定。
8. 搜索结果依赖默认体验，没有针对面试场景做二次组织。

## 3. 用户目标与关键流程

网站不应该只按内容分类设计，还应该按用户任务设计。建议明确以下 6 类主流程。

### 3.1 新手补基础流程

典型用户：刚接触测试开发或转岗用户。

当前问题：

- 用户可能直接进入 `pytest`、`api-assertion` 等内容，但不知道它在整体知识框架中的位置。
- `beginner` 和 `interview` 难度虽然存在，但没有形成引导式路径。

建议流程：

1. 首页选择“从基础开始”。
2. 进入基础路径页。
3. 按顺序学习：术语基础 -> 技术基础 -> 练手模板 -> 简单场景题。
4. 每篇学完后给出下一篇推荐。
5. 达到一定完成数后推荐进入 3 天或 7 天面试路线。

应新增或调整：

- 首页增加“我刚开始准备”入口。
- `/roadmap/beginner-foundation/` 或在现有路线中加入基础路径分支。
- 内容 schema 增加 `stage` 或 `learningPhase` 字段。

### 3.2 短期面试冲刺流程

典型用户：3 到 7 天内要面试。

当前问题：

- 首页有 3 天和 7 天路线，但内容页之间没有强制形成冲刺顺序。
- 用户很容易从路线页跳出去后迷失。

建议流程：

1. 首页选择 3 天速记或 7 天面试。
2. 路线页展示每日任务、预计耗时、完成状态。
3. 每个任务绑定若干内容页。
4. 内容页底部显示“回到今日任务”和“下一项任务”。
5. 结束后进入自测或模拟面试。

应新增或调整：

- 路线页从普通文档升级为任务清单页。
- 支持路线任务数据结构，例如 `src/lib/learning-paths.ts`。
- 内容页知道自己属于哪些路线。

### 3.3 项目经验打磨流程

典型用户：已经懂一些技术，但项目表达弱。

当前问题：

- `project` 模块有内容，但和 `scenario`、`interview-chains` 的连接不足。
- 用户看完一个项目页后，不知道应该补哪些术语、技术和追问。

建议流程：

1. 首页选择“打磨项目经验”。
2. 进入项目地图，按业务域选择：电商、支付、后台、SaaS、数据平台等。
3. 项目页提供“项目表达骨架”。
4. 页面底部推荐关联场景题、技术专题、追问链。
5. 进入模板页生成自己的项目故事。

应新增或调整：

- `/project/` 总览页。
- 每个项目页补 `relatedSlugs`，关联 `scenario`、`tech`、`glossary`、`interview-chains`。
- `practice-template/project-story-template` 作为所有项目页的固定后续入口。

### 3.4 技术专题复习流程

典型用户：需要复习 Python、Pytest、Playwright、接口测试、CI/CD 等技术。

当前问题：

- `tech` 内容较多，但缺少推荐顺序。
- 技术专题与编码题、场景题之间连接不强。

建议流程：

1. 首页选择“技术专题”。
2. 进入 `/tech/` 总览。
3. 按“基础技术、自动化框架、工程化、专项能力”分组。
4. 每个技术页底部给出“概念补充、编码练习、场景追问”。

应新增或调整：

- `/tech/` 总览页。
- 技术页增加“建议先学”和“学完练这个”。
- `tech/playwright` 关联 `practice-template/playwright-template` 和 `coding/fixture-strategy`。

### 3.5 高频场景题训练流程

典型用户：面试中经常被追问场景设计。

当前问题：

- `scenario` 是高价值模块，但首页只给一个推荐入口。
- 场景题之间没有按风险类型组织。

建议流程：

1. 首页选择“刷场景题”。
2. 进入 `/scenario/` 场景地图。
3. 按风险维度分组：并发、权限、支付、异步、数据迁移、第三方依赖、搜索推荐、报表导出。
4. 每个场景页底部推荐“对应术语、技术实现、追问链”。

应新增或调整：

- `/scenario/` 总览页。
- 场景页补跨模块推荐。
- 自测题可增加“回答评分点”，不只是选择题。

### 3.6 回访复盘流程

典型用户：已经看过一些内容，回来继续学习。

当前问题：

- 首页有最近浏览和进度，但位置偏后。
- 收藏没有列表页。
- 进度只显示全站总数，没有模块维度。

建议流程：

1. 回到首页首先看到“继续学习”。
2. 展示最近浏览、收藏、未完成路线任务。
3. 给出下一步推荐。
4. 支持按模块查看完成度。

应新增或调整：

- 首页首屏后增加“继续学习”区。
- 新增 `/bookmarks/` 页面。
- 学习进度按模块统计。
- 最近浏览也可出现在内容页侧栏或底部。

## 4. 信息架构调整建议

### 4.1 从 9 个平行模块改成 4 层结构

当前 9 个模块平铺在侧边栏里，对熟悉站点的人很清楚，但对新用户认知成本较高。建议在首页和总览页中建立更高层的 4 类结构。

建议结构：

1. 入门认知层
   - glossary
   - roadmap
   - practice-template 的部分基础模板

2. 技术能力层
   - tech
   - coding
   - practice-template 的工程模板

3. 项目与场景层
   - project
   - scenario
   - interview-chains

4. 新趋势与加分项
   - ai-learning
   - 高级项目表达
   - 高阶追问链

这样做的好处：

- 首页不只是展示 9 个模块，而是告诉用户为什么要学这些模块。
- 模块之间关系更清楚。
- 后续可以为不同用户目标生成路径。

### 4.2 建立模块总览页

建议为每个模块建立总览页：

- `/glossary/`
- `/tech/`
- `/project/`
- `/scenario/`
- `/coding/`
- `/roadmap/`
- `/ai-learning/`
- `/practice-template/`
- `/interview-chains/`

每个总览页建议包含：

- 模块定位：这个模块解决什么问题
- 学习建议：先看什么，后看什么
- 内容分组：按主题、难度或任务聚合
- 高频推荐：面试优先级高的内容
- 关联模块：学完后去哪里
- 完成进度：本模块已完成多少

示例：`/tech/` 可以分为：

- 编程基础：Python、Linux、Git
- 自动化框架：Pytest、Playwright
- 接口与数据：API 测试、数据库测试、测试数据管理
- 工程化：CI/CD、Docker、日志观测
- 专项能力：性能测试、消息队列、Redis、Mock

### 4.3 侧边栏导航重组

当前侧边栏顺序是：

1. 术语体系
2. 技术专题
3. 项目类型
4. 场景题
5. 编码题
6. 学习路线
7. AI 学习指南
8. 练手模板
9. 面试追问链
10. 分类索引

建议调整为更符合用户路径的顺序：

1. 学习路线
2. 术语体系
3. 技术专题
4. 编码题
5. 项目类型
6. 场景题
7. 面试追问链
8. 练手模板
9. AI 学习指南
10. 分类索引

理由：

- 用户先选路线，再进入知识。
- 编码题应靠近技术专题。
- 项目、场景、追问链属于面试表达层，应连续出现。
- AI 学习更像加分项，可放后面。

### 4.4 首页模块卡片不应只跳推荐单篇

当前首页核心模块卡片跳到每个模块的推荐单篇。建议改成：

- 主点击：进入模块总览页
- 卡片内增加“推荐起点”：跳推荐单篇

例如：

- 技术专题卡片主链接：`/tech/`
- 推荐起点：`/tech/pytest/`

这样能兼顾新用户和老用户：

- 新用户先看模块地图
- 老用户可以直接进高频内容

### 4.5 增加任务型入口

首页除了“学习路线”和“核心模块”，建议增加任务型入口：

- 我是新手，从基础开始
- 3 天后面试，快速冲刺
- 我要打磨项目经验
- 我要刷场景题
- 我要练编码题
- 我要准备 AI 相关追问

这些入口不一定都要新建页面，初期可以映射到已有路线或模块总览。

## 5. 首页改善建议

### 5.1 首页角色从“入口页”升级为“学习控制台”

当前首页主要作用是介绍和跳转。建议逐步升级为学习控制台。

推荐结构：

1. Hero 区
   - 保留站点定位和两个核心 CTA。
   - CTA 建议改为“选择学习路径”和“继续上次学习”。

2. 继续学习区
   - 最近浏览
   - 收藏入口
   - 未完成路线任务
   - 下一步推荐

3. 学习路径区
   - 新手基础路径
   - 3 天速记路径
   - 7 天面试路径
   - 项目打磨路径

4. 模块地图区
   - 9 个模块，但按 4 层结构分组。

5. 高频面试区
   - 高频术语
   - 高频场景
   - 高频追问链

6. 进度概览区
   - 全站进度
   - 模块进度
   - 已完成自测题数量

### 5.2 首屏 CTA 调整

当前 CTA：

- 开始学习
- 浏览术语

建议改为：

- 选择学习路径
- 继续上次学习

原因：

- “开始学习”过于宽泛。
- “浏览术语”会把用户引向单一模块，不一定符合所有用户目标。
- “继续上次学习”更适合回访用户。

### 5.3 最近浏览和进度上移

当前最近浏览和进度在核心模块之后，建议对回访用户上移。可以用条件显示：

- 如果有最近浏览或进度：放在学习路线前。
- 如果没有最近浏览：显示推荐路径。

### 5.4 首页要展示模块间关系

当前模块卡片是并列的。建议用简短流程表达：

基础术语 -> 技术专题 -> 编码练习 -> 项目经验 -> 场景题 -> 面试追问

这样用户能理解为什么有这些模块。

## 6. 内容页流程改善建议

### 6.1 内容页底部应从“操作区”升级为“下一步区”

当前内容页底部有：

- 标记完成
- 收藏
- 分享
- 自测题
- 默认分页

这些功能是有用的，但没有形成“学完这一篇要做什么”的明确引导。

建议底部结构：

1. 完成区
   - 标记完成
   - 收藏
   - 分享

2. 自测区
   - 当前选择题
   - 答错后展示解释
   - 完成后给出下一步

3. 下一步推荐
   - 相关术语
   - 相关技术
   - 相关场景
   - 相关追问链

4. 回到路径
   - 如果该内容属于某条路线，显示“回到 3 天路线”或“继续第 2 天任务”。

### 6.2 补齐跨模块 `relatedSlugs`

当前只有 glossary 有大量关联关系，其他模块基本没有。建议逐步补齐：

项目页应关联：

- 相关技术
- 相关术语
- 相关场景
- 相关追问链
- 项目故事模板

技术页应关联：

- 前置术语
- 编码练习
- 场景应用
- 练手模板

场景页应关联：

- 核心术语
- 技术实现
- 项目案例
- 追问链

编码题应关联：

- 技术专题
- 场景应用
- 模板页

追问链应关联：

- 原始技术页
- 项目页
- 场景页

### 6.3 内容页顶部增加“本文适合谁”

对面试准备站来说，用户很关心“我现在该不该读这篇”。建议在内容页元信息中展示：

- 难度：入门 / 面试
- 面试权重：1 到 3
- 预计阅读时间
- 适合阶段：基础 / 冲刺 / 项目表达 / 查漏补缺

### 6.4 自测题从“题目”升级为“面试表达练习”

当前自测题偏选择题。建议逐步增加：

- 回答骨架题：给出 3 段式回答结构
- 关键词检查：用户是否能说出核心关键词
- 追问题：面试官可能继续问什么
- 错误回答辨析：为什么这个说法不稳

不一定一开始做输入型功能，可以先在内容里结构化呈现。

### 6.5 内容页应减少重复渲染带来的结构复杂度

当前 `CustomContentPanel.astro` 里有对 Starlight 双 ContentPanel 的处理，例如通过 `:has(.sl-markdown-content)` 隐藏重复操作区。这个做法能工作，但结构上较脆弱。

建议后续评估：

- 是否能使用更稳定的 Starlight slot 或组件覆盖点。
- 是否能把“标题面板”和“正文面板”的增强逻辑分离。
- 是否需要针对重复 quiz 增加更明确的测试。

## 7. 搜索、索引与发现流程

### 7.1 搜索结果应面向面试任务增强

当前搜索依赖 Starlight / Pagefind 默认能力。建议补充搜索后的引导：

- 搜索“支付”时推荐支付项目、支付回调、幂等、支付追问链。
- 搜索“Playwright”时推荐技术页、模板页、相关编码题。
- 搜索“CI”时推荐 CI/CD、质量门禁、冒烟测试。

短期实现方式：

- 增加“高频搜索词推荐”区。
- 首页或搜索旁边放常用关键词。

中期实现方式：

- 建立 `term-registry` 与搜索词映射。
- 搜索页展示“相关路径”。

### 7.2 标签页改为筛选页

当前 `/tags/` 是按标签分组的长列表。建议改成更可用的筛选页：

- 顶部标签云，按数量或权重排序。
- 点击标签后只展示该标签内容。
- 支持按模块过滤。
- 支持按难度过滤。
- 展示每个标签下的内容数量。

### 7.3 难度页改为学习阶段页

当前 `/difficulty/` 只有入门和面试两个列表。建议扩展为：

- 入门基础
- 面试高频
- 项目表达
- 场景设计
- 编码训练
- 加分项

如果暂时不改 schema，也可以在页面上按 category 二次分组，让列表更可扫。

### 7.4 增加站点地图式入口

建议增加 `/map/` 或 `/topics/`：

- 显示全部模块
- 显示每个模块内容数量
- 显示推荐顺序
- 支持按难度和权重筛选

这个页面可以作为 SEO 和用户发现的双重入口。

## 8. 学习进度、收藏与回访流程

### 8.1 新增收藏列表页

当前收藏按钮是半闭环：用户能收藏，但没有地方集中查看。

建议新增 `/bookmarks/`：

- 读取 `testdev:bookmarks`
- 展示收藏内容标题、模块、难度
- 支持移除收藏
- 空状态推荐“先收藏高频主题”

### 8.2 进度按模块统计

当前首页进度是全站总数。建议增加模块维度：

- 术语体系：已完成 x / 33
- 技术专题：已完成 x / 19
- 项目类型：已完成 x / 12
- 场景题：已完成 x / 14

好处：

- 用户知道自己偏科在哪里。
- 后续路线推荐更智能。

### 8.3 最近浏览作为全局回访入口

当前最近浏览只在首页展示。建议：

- 内容页底部增加最近浏览入口。
- 标签页和难度页侧边展示最近浏览。
- 首页将最近浏览提前。

### 8.4 下一步推荐算法

短期可以基于规则：

1. 有路线任务，优先推荐路线下一项。
2. 当前页面有 `relatedSlugs`，推荐第一个未完成关联内容。
3. 当前模块未完成，推荐同模块下一篇。
4. 没有上下文时，推荐首页配置的高频内容。

中期可以加入：

- 难度递进
- 面试权重
- 用户已完成模块
- 最近浏览类别

## 9. 路线系统改善建议

### 9.1 路线应从 Markdown 文档升级为数据驱动

当前路线是内容页。建议抽象为数据：

```ts
interface LearningPath {
  id: string;
  title: string;
  description: string;
  audience: string;
  days: Array<{
    title: string;
    goal: string;
    tasks: Array<{
      label: string;
      href: string;
      expectedMinutes: number;
      required: boolean;
    }>;
  }>;
}
```

好处：

- 首页可展示路线进度。
- 内容页可知道自己属于哪条路线。
- 后续可以生成路线页、任务清单和 E2E 测试。

### 9.2 建议新增 5 条路径

除现有 3 天和 7 天路线外，建议增加：

1. 新手基础路径
   - 目标：建立测试开发基础概念。
   - 内容：术语、Python、Pytest、API 测试、测试设计。

2. 接口自动化路径
   - 目标：准备接口测试和自动化框架类面试。
   - 内容：API 测试、断言、数据驱动、Mock、CI/CD、报告。

3. UI 自动化路径
   - 目标：准备 Playwright / UI 自动化面试。
   - 内容：Playwright、Page Object、Fixture、稳定性、兼容性。

4. 项目表达路径
   - 目标：把项目经验讲完整。
   - 内容：项目类型、项目故事模板、质量门禁、场景题。

5. 高阶追问路径
   - 目标：应对深入追问。
   - 内容：interview-chains、scenario、AI learning、高阶技术专题。

### 9.3 每条路径应有完成标准

示例：

- 完成所有必读内容。
- 完成每篇自测题。
- 收藏至少 3 个需要复盘的主题。
- 能用 2 分钟讲清一个项目。
- 能回答 5 个连续追问。

## 10. 内容治理与编辑规范

### 10.1 增加内容生命周期字段

建议在 frontmatter 中逐步增加：

- `stage`: foundation / practice / interview / advanced
- `estimatedMinutes`
- `prerequisites`
- `outcomes`
- `nextSlugs`
- `pathIds`
- `lastReviewed`

这些字段可以先 optional，逐步补齐。

### 10.2 统一内容模板

不同模块建议使用不同模板。

术语页：

- 是什么
- 为什么重要
- 面试怎么说
- 易错点
- 混淆概念
- 自测题
- 关联内容

技术页：

- 学什么
- 为什么面试会问
- 核心概念
- 项目落地
- 常见追问
- 代码或实践示例
- 关联练习

项目页：

- 项目背景
- 核心业务流程
- 测试重点
- 自动化策略
- 风险与故障处理
- 数据和环境
- 面试表达模板
- 关联场景

场景页：

- 场景问题
- 风险分析
- 测试维度
- 用例设计
- 自动化策略
- 监控和回滚
- 面试回答骨架

编码题：

- 题目
- 考察点
- 设计思路
- 代码骨架
- 边界条件
- 可扩展点
- 常见追问

追问链：

- 起始问题
- 追问路径
- 每一步考察点
- 回答策略
- 高风险错误回答
- 关联知识补充

### 10.3 标签体系需要收敛

当前标签数量和分布较丰富，但长期会有标签泛化和重复风险。建议：

- 建立标签白名单。
- 标签分为能力、技术、场景、项目、面试策略 5 类。
- 避免同义标签并存，例如 API、接口测试、API 测试需要统一规则。
- 内容校验脚本检查未知标签。

### 10.4 关联关系不应只服务 glossary

当前其他模块 `relatedSlugs` 为空。建议设置最低治理规则：

- 每篇技术页至少 2 个关联。
- 每篇项目页至少 3 个关联。
- 每篇场景页至少 3 个关联。
- 每篇编码题至少 2 个关联。
- 每条追问链至少 3 个关联。

可以先作为 warning，不立即设为 error。

### 10.5 模块内容组织规范

每个模块的详细内容组织规则已经补充到
[`MODULE_CONTENT_ORGANIZATION_GUIDE.md`](./MODULE_CONTENT_ORGANIZATION_GUIDE.md)。

后续新增、重写或验收内容时，以这份规范作为模块层面的主参考：

- `beginner-course` 负责低门槛教学路线。
- `glossary` 负责概念补洞。
- `tech` 负责技术理解和项目落地。
- `project` 负责项目经验组织。
- `scenario` 负责业务风险和测试设计。
- `coding` 负责小型工程能力训练。
- `roadmap` 负责学习顺序和面试打法。
- `ai-learning` 负责 AI 工具、边界和质量责任。
- `practice-template` 负责可交付练习产物。
- `interview-chains` 负责连续追问训练。

这份规范也定义了每个模块的总览页结构、单篇内容结构、练习规则、`relatedSlugs`
规则和验收标准，能弥补当前建议文档中“模板方向有了，但模块级组织细节不足”的问题。

## 11. 导航和布局层改善建议

### 11.1 统一非 Starlight 页面布局

当前首页、标签页、难度页使用 `MainLayout`，但该布局不包含统一 Header。建议：

- `MainLayout` 内置全站 Header。
- 或新增 `MarketingLayout` / `IndexLayout`，统一首页和索引页壳层。
- 保证首页、标签页、难度页、未来收藏页都有一致导航。

### 11.2 顶部导航建议

顶部导航建议包含：

- 首页
- 学习路线
- 技术专题
- 项目经验
- 场景题
- 编码题
- 收藏
- 搜索

移动端：

- 保留站点名
- 搜索按钮
- 菜单按钮
- 最近学习入口

### 11.3 面包屑增强

Starlight 自带面包屑，但可以补充：

- 模块总览链接
- 所属学习路径链接
- 当前内容的阶段标签

示例：

首页 > 技术专题 > 接口测试路径 > API 测试

### 11.4 404 页面增强

当前 404 应保证不只是默认错误页。建议：

- 提供搜索入口
- 推荐 3 天路线
- 推荐热门模块
- 提供回首页按钮

## 12. SEO 与外部访问流程

### 12.1 每个模块总览页有 SEO 价值

模块总览页不仅改善用户体验，也适合搜索引擎理解站点结构。

建议每个总览页有：

- 明确 H1
- 模块说明
- 内容列表
- FAQ 或高频问题
- 结构化数据

### 12.2 内容页结构化数据增强

已有 `JsonLd` 组件。建议后续增加：

- Article
- BreadcrumbList
- FAQPage，适用于问答型内容
- LearningResource，适用于路线和模板

### 12.3 Open Graph 与分享页体验

当前 OG 图是统一图片。建议长期改进：

- 模块页有模块级 OG 描述。
- 高频页面有更具体的标题和描述。
- 分享文案区分术语、项目、场景、追问链。

### 12.4 Sitemap 分层

当前 Astro sitemap 可生成基础 sitemap。建议：

- 确保模块总览页进入 sitemap。
- 高频页面设置更高优先级。
- 可选：生成 `llms.txt` 或内容索引文件，方便 AI 工具理解站点结构。

## 13. 可访问性和移动端流程

### 13.1 移动端优先检查路径

面试准备用户很可能在手机上碎片化复习。建议重点验证：

- 首页 Hero 是否占用过多首屏
- 最近浏览是否能快速点击
- 侧边栏是否容易打开和关闭
- 自测题按钮是否够大
- 收藏和标记完成是否误触
- 长内容目录是否可用

### 13.2 键盘和读屏流程

建议增加：

- 跳过导航链接
- 搜索按钮 aria-label
- 收藏按钮状态读屏友好
- 自测题结果读屏提示
- 进度条使用 `aria-valuenow`

### 13.3 高对比度和主题一致性

当前 V3 设计系统已经有基础。后续注意：

- 标签页、难度页等非核心页面要同步 V3 视觉。
- 按钮和链接 hover/focus 状态一致。
- 不要让索引页看起来像旧系统残留。

## 14. 工程结构与组件职责

### 14.1 清理旧组件残留

建议审查：

- `RecentViews.astro`
- `SidebarProgress.astro`
- `Banner.astro`

其中 `RecentViews.astro` 与当前 `src/lib/recent-views.ts` 的职责重叠，且未接入主流程。`SidebarProgress.astro` 实际做的是当前页高亮，不是进度。建议：

- 删除未使用组件。
- 或重命名为真实职责。
- 为保留组件补使用场景和测试。

### 14.2 本地状态统一

当前状态相关逻辑包括：

- `storage.ts`
- `progress-store.ts`
- `bookmark-store.ts`
- `recent-views.ts`
- 首页内联进度读取逻辑

建议：

- 首页进度也改用 `progress-store.ts`。
- 所有 localStorage schema 统一定义。
- 增加迁移和容错策略。
- 单测覆盖 malformed storage。

### 14.3 路径生成统一

当前多处会生成 `${base}${category}/${slug}/`。建议统一：

- `buildContentHref(base, category, slug)`
- `buildStaticHref(base, page)`
- `normalizeBase(base)`

好处：

- 减少 GitHub Pages base path 错误。
- 让链接校验更容易复用。

## 15. 测试与质量门建议

### 15.1 增加结构流程测试

建议新增 E2E：

- 首页模块卡片都能进入总览页或推荐页。
- 从路线页进入第一篇内容，再返回路线。
- 收藏一篇内容后能在收藏页看到。
- 标记完成后模块进度更新。
- 标签页筛选后列表正确。
- 难度页按模块分组正确。

### 15.2 增加内容结构校验

当前内容校验已经覆盖：

- category
- relatedSlugs
- selfTests
- body length
- recommendedSlug
- Markdown 站内链接

建议继续增加：

- 标签白名单
- 每篇内容必须有 description
- 每篇内容必须有 difficulty
- 每篇内容必须有 interviewWeight
- 非 glossary 内容至少有若干 relatedSlugs
- title 不能重复
- slug 不能跨模块产生歧义，或歧义必须显式处理

### 15.3 增加可视化回归抽样

建议对关键页面做截图回归或视觉检查：

- 首页
- 技术总览页
- 内容页
- 标签页
- 难度页
- 收藏页
- 404 页

### 15.4 定期全量 E2E

当前 CI 跑 Chromium smoke。建议：

- push main 继续跑 smoke。
- 每周定时跑 full e2e，覆盖 Chromium / Firefox / WebKit。
- 定时检查线上首页和 sitemap。

## 16. 数据指标与效果评估

如果后续引入隐私友好的分析，可关注：

### 16.1 导航指标

- 首页 CTA 点击率
- 模块总览页点击率
- 搜索使用率
- 标签页使用率
- 难度页使用率

### 16.2 学习指标

- 平均完成主题数
- 自测题完成率
- 收藏使用率
- 最近浏览回访率
- 路线完成率

### 16.3 内容指标

- 高频页面访问量
- 搜索无结果关键词
- 退出率高的页面
- 从项目页跳到场景页的比例

### 16.4 面试准备指标

可以不做在线账号，也可以通过本地状态估算：

- 用户是否完成一条路径
- 是否完成某模块 80%
- 是否收藏了复盘内容
- 是否完成追问链

## 17. 分阶段实施路线

### 17.1 第一阶段：结构地基，1 到 2 天

目标：让用户不迷路。

任务：

1. 统一首页、标签页、难度页的 Header 和基础导航。
2. 调整侧边栏模块顺序。
3. 清理或重命名未使用组件。
4. 抽统一 href 生成 helper。
5. 首页 CTA 改为“选择学习路径”和“继续上次学习”。

验收：

- `npm run check` 通过。
- Chromium smoke E2E 通过。
- 首页、标签页、难度页都有一致导航。

### 17.2 第二阶段：模块总览，3 到 5 天

目标：让用户知道每个模块怎么学。

任务：

1. 增加 9 个模块总览页。
2. 总览页按难度、面试权重、主题分组。
3. 首页模块卡片跳总览页。
4. 模块总览页展示推荐起点。
5. 增加 E2E 覆盖模块总览入口。

验收：

- 首页所有模块入口可访问。
- 每个总览页至少展示模块定位、推荐顺序、内容列表。
- sitemap 包含总览页。

### 17.3 第三阶段：学习闭环，3 到 7 天

目标：让用户读完一篇后知道下一步。

任务：

1. 为非 glossary 内容补 `relatedSlugs`。
2. 内容页底部增加“下一步推荐”。
3. 新增收藏列表页。
4. 首页继续学习区上移。
5. 进度按模块统计。

验收：

- 主要内容页都有相关内容推荐。
- 收藏后可在收藏页查看。
- 首页能展示模块进度。

### 17.4 第四阶段：路线系统，5 到 10 天

目标：让站点变成真正的面试训练系统。

任务：

1. 建立数据驱动 learning paths。
2. 改造 3 天和 7 天路线为任务清单。
3. 新增基础路径、项目路径、场景路径、编码路径。
4. 内容页显示所属路线。
5. 路线页显示完成状态。

验收：

- 用户可从路径页连续完成任务。
- 内容页可回到路线。
- 路线完成度可保存到 localStorage。

### 17.5 第五阶段：索引和搜索增强，3 到 5 天

目标：让用户更快找到内容。

任务：

1. 标签页升级为筛选页。
2. 难度页升级为学习阶段页。
3. 增加高频搜索词入口。
4. 增加站点地图页。
5. 搜索无结果时给推荐入口。

验收：

- 用户可按标签、模块、难度组合查找。
- 站点地图清楚展示全部内容。

## 18. 优先级矩阵

| 优先级 | 建议                    | 用户价值 | 工程成本 | 风险 |
| ------ | ----------------------- | -------: | -------: | ---: |
| P0     | 统一全站 Header         |       高 |       中 |   低 |
| P0     | 首页继续学习上移        |       高 |       低 |   低 |
| P0     | 清理未使用组件          |       中 |       低 |   低 |
| P1     | 模块总览页              |     很高 |       中 |   中 |
| P1     | 收藏列表页              |       高 |       中 |   低 |
| P1     | 内容页下一步推荐        |     很高 |       中 |   中 |
| P1     | 补全跨模块 relatedSlugs |     很高 |       高 |   低 |
| P2     | 路线数据化              |     很高 |       高 |   中 |
| P2     | 模块进度统计            |       高 |       中 |   中 |
| P2     | 标签页筛选化            |       中 |       中 |   低 |
| P2     | 难度页阶段化            |       中 |       中 |   低 |
| P3     | 搜索结果增强            |       中 |       高 |   中 |
| P3     | 结构化数据扩展          |       中 |       中 |   低 |
| P3     | 定期全量 E2E            |       中 |       低 |   低 |

## 19. 建议首先落地的 10 个具体任务

1. 统一 `MainLayout` 的 Header。
2. 调整 Starlight sidebar 顺序，把学习路线前置。
3. 删除或重命名 `RecentViews.astro`、`SidebarProgress.astro` 等职责不清组件。
4. 新增 `src/lib/links.ts`，统一生成站内 href。
5. 首页增加“继续学习”区，最近浏览和进度前置。
6. 新增 `/bookmarks/` 页面。
7. 新增 `/tech/` 模块总览页，作为总览页样板。
8. 为 `tech/api-testing`、`project/payment-project`、`scenario/payment-callback`、`interview-chains/api-testing-chain` 建立跨模块关联样板。
9. 内容页底部新增“下一步推荐”组件。
10. 增加 E2E：从首页进入模块总览，再进入内容页，再返回路线或推荐页。

## 20. 风险与注意事项

### 20.1 不要一次性重构所有页面

站点内容多，结构调整容易影响大量页面。建议用样板推进：

1. 先做 `/tech/` 总览页。
2. 验证结构和样式。
3. 再复制到其他模块。

### 20.2 不要让首页重新变成营销页

首页应该服务学习路径和回访，不应做成大而空的宣传页。Hero 应克制，重点放在任务入口。

### 20.3 不要过早引入账号系统

当前 localStorage 足够支持个人学习闭环。账号系统会显著增加复杂度，不建议近期做。

### 20.4 不要让标签泛滥

标签越多，索引越难用。应先治理标签，再做复杂筛选。

### 20.5 不要把所有推荐都写死在组件里

推荐关系应该来自内容 frontmatter 或统一数据文件，避免散落在 Astro 组件中。

## 21. 最终建议

如果只做一条主线，建议选择：

首页学习控制台 -> 模块总览页 -> 内容页下一步推荐 -> 收藏和进度闭环 -> 数据化学习路线

这条路线最符合当前站点的产品目标：帮助用户在短时间内补齐测试开发面试知识，并把知识转化成可表达、可练习、可复盘的面试能力。

当前项目已经具备较好的内容和工程基础，下一阶段最值得投入的不是继续堆内容，而是把已有内容组织成更明确的学习流程。只要把模块总览、跨模块关联、回访入口和路线任务化做好，站点会从“资料库”明显升级为“面试训练工具”。
