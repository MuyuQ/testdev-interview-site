---
title: "UI 自动化"
description: "通过自动驱动浏览器或客户端验证关键用户路径，适合覆盖高价值回归场景。"
category: "glossary"
difficulty: "interview"
interviewWeight: 2
tags: ["UI 自动化", "回归测试", "稳定性", "端到端测试"]
relatedSlugs: ["page-object-pattern", "selenium", "playwright"]
selfTests:
  - id: "ui-automation-1"
    question: "UI 自动化最适合覆盖什么场景？"
    options:
      - "高价值、稳定、重复执行的核心用户路径"
      - "所有一次性页面样式调整"
      - "所有后端单元逻辑"
      - "无需断言的手工操作"
    correctIndex: 0
    explanation: "UI 自动化成本较高，应优先覆盖稳定且业务价值高的回归路径。"
  - id: "ui-automation-2"
    question: "提升 UI 自动化稳定性的关键是什么？"
    options:
      - "稳定定位、合理等待、独立数据和失败诊断"
      - "只用固定 sleep"
      - "把所有用例串行执行"
      - "不做任何断言"
    correctIndex: 0
    explanation: "UI 自动化稳定性依赖定位、等待、数据、隔离和诊断能力。"
---

UI 自动化是通过工具自动驱动浏览器或客户端，验证用户在界面上的关键操作路径。它通常用于核心流程回归，例如登录、下单、支付、审批、配置保存和报表导出。它最接近用户视角，但执行成本和维护成本也相对更高。

好的 UI 自动化策略不是“页面都录一遍”，而是选择稳定、高价值、重复执行的场景。低价值、频繁变化、视觉细节很强的场景更适合人工检查或视觉回归工具。测试开发要能说明为什么某些用例放在 UI 层，某些用例下沉到接口层或单元层。

UI 自动化的稳定性来自工程设计。定位要优先使用可访问角色、稳定测试标识或业务语义；等待要基于状态变化，而不是固定 sleep；数据要独立可控；失败时要有截图、视频、Trace、网络日志或控制台日志帮助定位。

面试回答时可以强调分层：UI 自动化只保留关键端到端路径，大量业务组合放到接口自动化，纯逻辑放到单元测试。这样既保证用户路径可回归，又不会让测试套件慢到无法维护。
