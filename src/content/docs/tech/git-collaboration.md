---
title: "Git 版本协作"
description: "把分支管理、冲突解决和协作规范作为团队开发基础能力。"
category: "tech"
difficulty: "beginner"
interviewWeight: 1
tags: ["Git", "分支管理", "协作", "版本控制"]
selfTests:
  - id: "git-collaboration-tech-1"
    question: "测试开发掌握 Git 的核心价值是什么？"
    options:
      - "只会下载代码"
      - "让测试代码、数据、配置和报告模板都可追踪、可评审、可回滚"
      - "避免写测试"
      - "替代 CI"
    correctIndex: 1
    explanation: "Git 不只是代码下载工具，测试资产也应版本化管理，保证变更可追踪、可评审和可回滚。"
  - id: "git-collaboration-tech-2"
    question: "解决冲突时最不应该做什么？"
    options:
      - "理解双方改动意图"
      - "解决后运行测试"
      - "直接覆盖另一方代码"
      - "同步最新基础分支"
    correctIndex: 2
    explanation: "冲突解决不能简单覆盖，要理解两边意图，合并后运行相关测试确认行为正确。"
  - id: "git-collaboration-tech-3"
    question: "自动化代码与业务版本如何对齐？"
    options:
      - "通过同仓分支或独立仓库标签建立业务版本与测试版本映射"
      - "不需要对齐"
      - "只看文件名"
      - "靠人工记忆"
    correctIndex: 0
    explanation: "自动化代码要和业务版本建立明确映射，避免用新脚本测试旧业务或旧脚本漏测新功能。"
---

## 说明

Git 是团队协作的版本控制基础，测试开发需要掌握分支管理、代码合并、冲突解决和协作规范。

## 面试怎么问

### 你们团队的 Git 分支策略是什么？

使用 Git Flow 或简化版的分支策略：main 分支是生产代码，develop 分支是开发基线，feature 分支开发新功能，release 分支准备发布，hotfix 分支修复紧急问题。测试代码遵循相同策略，与业务代码分支对应。

关键是每个分支的用途和合并规则要清晰。

### 自动化代码怎么和业务代码协作管理？

两种模式：

一是自动化代码与业务代码同仓库，分支策略一致，代码评审流程统一。

二是自动化代码独立仓库，按版本标签对应业务版本。

选择依据是团队协作模式，关键是版本对齐机制，确保自动化代码与业务版本匹配。

### 遇到冲突时你怎么处理？

首先理解冲突原因：是并行开发还是基础分支更新。

处理方式：优先 rebase 到最新基础分支，再解决冲突。冲突解决要理解两边代码意图，不能简单覆盖。解决后要运行测试验证。预防冲突：及时同步基础分支更新、合理规划开发任务、减少长生命周期分支。

## 项目里怎么用

- 按功能或迭代分支管理自动化脚本和配置，分支命名有规范。
- 通过 PR 机制做代码评审和合并，保证代码质量。
- 把测试用例和数据文件纳入版本控制，变更可追溯。
- CI 触发基于分支规则，如 develop 分支触发集成测试。
- 定期清理过期分支，保持仓库整洁。

## 协作规范补充

测试开发的 Git 协作不只管理自动化脚本，还要管理测试数据、Mock 配置、CI 配置、报告模板和工具代码。任何会影响测试结果的资产都应该进入版本控制，避免只有某个人本地有一份关键文件。

分支策略要和业务发布节奏对齐。如果业务功能在 feature 分支开发，自动化脚本也应在对应分支维护；如果业务发布使用 release 分支，测试脚本也要在 release 前完成回归适配。这样可以避免主干脚本和待发布功能错位。

## 风险点拆解

### 测试代码和业务版本错位

自动化脚本过早合入或滞后更新，都会导致误判。新脚本跑旧环境可能失败，旧脚本跑新功能又可能漏测。测试团队要建立版本映射，例如同仓库同分支、独立仓库使用 tag 或配置文件记录业务版本。

### 测试数据变更不可追溯

很多自动化失败来自测试数据修改，但如果数据文件没有评审和版本记录，很难定位是谁改了什么。数据驱动文件、环境配置和账号池都要纳入 PR 流程。

### 冲突处理破坏测试逻辑

测试代码冲突经常发生在公共 fixture、登录封装、接口客户端和配置文件。解决冲突时不能只让代码编译过，还要运行相关测试，确认公共封装行为没有被破坏。

## 面试追问

### 你提交自动化代码前会做什么？

我会先同步最新基础分支，运行本地相关测试和格式检查，确认没有提交敏感配置，再发起 PR。PR 里说明改动范围、影响模块、验证结果和是否需要同步测试数据。

### 自动化脚本和业务代码分仓有什么注意点？

分仓时最重要的是版本对齐。可以用业务版本号、接口版本、发布分支或环境配置建立映射；CI 里也要明确当前测试脚本对应哪个业务构建，避免测试结果不可解释。

## 容易答错什么

### 只会基本命令，不会讲分支策略和协作规范

Git 不只是 commit 和 push，分支策略决定了团队协作效率。要说明为什么选择这种策略、各种分支的生命周期、合并和删除规则。协作规范包括：commit message 格式、PR 评审流程、冲突解决原则。这些是团队协作的基础。

### 忽略自动化代码和业务代码的版本对齐问题

自动化代码落后于业务代码会导致测试失败，超前又可能覆盖不到新功能。

解决：版本标签对应、定期同步更新、自动化代码随业务版本一起发布。还要考虑多版本并行维护的场景。

### 不会讲 CI 中 Git 触发和分支过滤机制

CI 要根据分支策略配置触发规则：develop 分支合并触发集成测试，release 分支触发预发布测试，main 分支触发生产部署。要说明如何在 CI 配置中设置分支过滤，如何配置不同分支的触发条件，以及如何处理多分支并行开发场景。
