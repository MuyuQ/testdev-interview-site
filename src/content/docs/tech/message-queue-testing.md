---
title: "消息队列测试"
description: "围绕消息投递、消费确认和异常补偿组织消息队列测试回答。"
category: "tech"
difficulty: "interview"
interviewWeight: 2
tags: ["消息队列", "MQ", "异步", "可靠性"]
selfTests:
  - id: "message-queue-testing-tech-1"
    question: "消息队列测试最核心的验证链路是什么？"
    options:
      - "消息是否进入队列、是否被消费、业务副作用是否正确"
      - "队列名称是否规范"
      - "只看生产者接口返回"
      - "只看消费者进程存在"
    correctIndex: 0
    explanation: "消息测试要验证投递、消费和消费后的业务副作用，单看生产者或消费者都不完整。"
  - id: "message-queue-testing-tech-2"
    question: "重复消费场景重点验证什么？"
    options:
      - "消费者日志数量"
      - "消费逻辑幂等，不重复写入或重复推进状态"
      - "消息体越短越好"
      - "队列永不积压"
    correctIndex: 1
    explanation: "MQ 常见重复投递或 ACK 丢失，因此消费者必须幂等，避免重复写入、重复扣款或重复通知。"
  - id: "message-queue-testing-tech-3"
    question: "死信队列测试主要验证什么？"
    options:
      - "失败消息超过重试次数后进入死信，并支持告警和人工/自动补偿"
      - "所有消息都进入死信"
      - "死信消息不需要处理"
      - "只验证队列存在"
    correctIndex: 0
    explanation: "死信队列用于承接无法正常消费的消息，测试要验证重试策略、入死信、告警和补偿闭环。"
---

## 说明

消息队列测试关注消息投递可靠性、消费确认、重复消费和异常补偿场景。

## 面试怎么问

### 你怎么验证消息是否成功投递和消费？

验证分三个层面：

投递层检查消息是否进入队列（检查队列消息数或消息 ID）。

消费层检查消费者是否收到并处理消息。

业务层检查消息消费后的副作用（数据库更新、业务状态变更）。

对于关键消息，要验证消费确认机制，确保消息不会丢失。

### 消息重复消费怎么测？

模拟重复投递场景：消费者重复收到同一消息、网络抖动导致 ACK 丢失、消费者重启后重新消费。

验证点：消费逻辑是否[幂等](/testdev-interview-site/glossary/idempotency/)、是否会重复写入数据、业务状态是否一致。

测试方法：手动发送重复消息、延迟 ACK、模拟网络问题。

### 消息积压和消费失败场景怎么处理？

消息积压时验证：积压告警是否触发、消费者是否能自动扩容、积压消息的优先级处理。消费失败时验证：是否有重试机制、重试次数和间隔是否合理、死信队列处理、人工补偿入口。还要验证监控和告警是否覆盖这些异常场景。

## 项目里怎么用

- 验证生产者投递成功和消费者确认机制，确保消息不丢失。
- 覆盖消息重复、顺序、延迟和丢失场景，验证系统的健壮性。
- 结合业务状态验证消息消费后的副作用，确保业务一致性。
- 测试死信队列和补偿机制，验证异常情况的处理。

## 风险点拆解

### 消息丢失

消息可能在生产者发送、Broker 存储、消费者消费确认三个阶段丢失。测试要明确系统使用的可靠性机制，例如发送确认、持久化、消费 ACK、重试和补偿。只看到接口返回成功，不代表消息已经可靠落到队列。

### 重复消费

重复消费比想象中常见。网络抖动、消费者重启、ACK 丢失、重试机制都可能让同一消息被处理多次。消费者必须用业务唯一键、幂等表、状态机或去重缓存保证重复消费不会造成重复扣款、重复发奖或重复通知。

### 顺序和延迟

有些业务依赖消息顺序，例如订单创建后才能支付成功，库存释放不能早于订单取消。测试要确认业务是否要求严格顺序，如果要求顺序，就要验证分区键、消费者并发和乱序补偿。

## 测试策略

- 投递验证：检查生产者发送结果、Broker 消息 ID、队列消息数和日志。
- 消费验证：检查消费者日志、ACK、业务状态、数据库副作用和下游通知。
- 异常验证：模拟消费者异常、网络中断、Broker 重启、重复消息、延迟消息和乱序消息。
- 积压验证：构造消费速度低于生产速度，验证积压告警、扩容、限流和恢复能力。
- 补偿验证：验证重试次数、间隔、死信队列、人工处理入口和处理后的二次校验。

## 面试追问

### 如何证明消息没有丢？

可以从三处证明：生产者有发送确认和日志，Broker 中能查到消息 ID，消费者处理后业务状态发生了预期变化。关键消息还应有对账或补偿任务，用最终业务状态证明链路闭环。

### 如何设计消息幂等？

一般用业务唯一键做幂等，例如订单号、支付单号、消息 ID。消费前先检查是否已处理，消费中用事务或状态机保护，消费后记录处理结果。测试要重复发送同一消息，确认业务状态只变化一次。

- 监控消息积压情况，建立告警机制。

## 容易答错什么

### 只关注投递，忽略消费确认和幂等

消息投递只是第一步，关键是消费确认和幂等处理。

消费确认确保消息不丢失，幂等处理确保重复消费不产生副作用。

测试要覆盖：ACK 超时重投递、消费者崩溃后重新消费、网络问题导致的重复。

这些场景在生产环境经常发生。

### 不会讲消息积压和消费异常的补偿机制

消息积压和消费异常是线上常见问题。补偿机制包括：死信队列存储失败消息、定时重试、人工处理入口、监控告警。要说明如何测试这些机制的有效性，以及如何设计测试用例覆盖各种异常场景。

### 忽略跨服务消息链路的验证

消息队列连接多个服务，要验证完整链路：生产者是否正确发送、消息内容是否正确、消费者是否正确处理、下游服务是否正确接收。可以使用链路追踪查看消息流转，验证每个环节的正确性。跨服务验证是 MQ 测试的核心难点。
