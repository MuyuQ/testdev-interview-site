---
import { getHomePageData } from "../lib/home-page";

const base = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
const { entryLinks, beginnerPath, capabilityLayers } = getHomePageData(base);
const beginnerEntry = entryLinks.find((entry) => entry.id === "beginner")!;
const interviewEntry = entryLinks.find((entry) => entry.id === "interview")!;
const initialPathSteps = beginnerPath.slice(0, 4);
const allModulesHref =
  capabilityLayers[0].modules[0]?.href ?? beginnerEntry.href;
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href={`${base}favicon.svg`} />
    <title>测试开发面试速成站</title>
    <meta
      name="description"
      content="面向测试开发学习者的结构化学习站，帮助你补齐基础、完成练习项目，并把技术思路整理成面试中能讲清楚的答案。"
    />
  </head>
  <body>
    <header class="site-header">
      <a class="site-brand" href={base} aria-label="测试开发面试速成站首页">
        <span class="site-brand-mark" aria-hidden="true">测</span>
        <span>测试开发面试速成站</span>
      </a>
      <nav class="site-nav" aria-label="主要导航">
        <a class="site-nav-link is-active" href={base} aria-current="page"
          >学习首页</a
        >
        <a class="site-nav-link" href={beginnerEntry.href}>新手教程</a>
        <a class="site-nav-link" href={capabilityLayers[1].modules[0].href}
          >技术专题</a
        >
        <a class="site-nav-link" href={capabilityLayers[2].modules[0].href}
          >项目与场景</a
        >
        <a class="site-nav-link" href={capabilityLayers[3].modules[0].href}
          >面试追问链</a
        >
        <a
          class="site-nav-action"
          href={beginnerPath[0].href}
          data-continue-learning>继续学习</a
        >
      </nav>
    </header>

    <main class="page-shell">
      <section class="hero-section" aria-labelledby="hero-title">
        <div class="hero-copy">
          <p class="hero-signal">从知识到面试表达</p>
          <h1 id="hero-title">找到下一步，把测试开发能力练出来</h1>
          <p class="hero-description">
            面向测试开发学习者的结构化学习站。按阶段补齐基础、完成练习项目，并把技术思路整理成面试中能讲清楚的答案。
          </p>
          <div class="hero-actions">
            <a class="button button-primary" href={beginnerEntry.href}>
              开始选择学习路径
              <span aria-hidden="true">→</span>
            </a>
            <a class="button button-secondary" href="#capability-map"
              >查看完整模块地图</a
            >
          </div>
        </div>

        <aside
          class="path-panel"
          data-learning-path
          aria-labelledby="path-panel-title"
        >
          <div class="path-panel-header">
            <div>
              <p class="path-panel-kicker">新手基础路径</p>
              <h2 id="path-panel-title">7 天完成第一个接口测试项目</h2>
            </div>
            <span class="status-tag" data-path-progress>从第 1 步开始</span>
          </div>

          <div class="current-step">
            <span class="current-step-label">当前建议学习</span>
            <a
              class="current-step-title"
              href={beginnerPath[0].href}
              data-current-step-link
              data-continue-learning
            >
              {beginnerPath[0].title}
            </a>
            <p data-current-step-outcome>{beginnerPath[0].outcome}</p>
            <span class="current-step-time" data-current-step-time
              >{beginnerPath[0].minutes} 分钟</span
            >
          </div>

          <ol class="path-list" aria-label="新手基础路径预览">
            {
              initialPathSteps.map((step, index) => (
                <li class:list={["path-item", { "is-current": index === 0 }]}>
                  <a
                    href={step.href}
                    data-path-slot
                    data-step-slug={step.slug}
                    aria-current={index === 0 ? "step" : undefined}
                  >
                    <span class="path-marker" aria-hidden="true" />
                    <span class="path-item-copy">
                      <strong data-slot-title>{step.title}</strong>
                      <span data-slot-outcome>{step.outcome}</span>
                    </span>
                    <span class="path-item-time" data-slot-time>
                      {step.minutes} 分钟
                    </span>
                  </a>
                </li>
              ))
            }
          </ol>

          <a class="path-panel-link" href={beginnerEntry.href}
            >查看完整 8 步路线 →</a
          >
        </aside>
      </section>

      <section class="decision-section" aria-labelledby="decision-title">
        <div class="section-heading">
          <div>
            <h2 id="decision-title">先判断你现在需要什么</h2>
            <p>不从十个分类开始，而是先用学习目标帮助你做决定。</p>
          </div>
        </div>

        <div class="mode-layout">
          <a class="mode-primary" href={beginnerEntry.href}>
            <div>
              <span class="mode-label">适合刚入门或知识零散的学习者</span>
              <h3>从零开始，建立一条能走完的学习路线</h3>
              <p>用 7 天完成基础知识、接口测试练习和第一个可讲述的小项目。</p>
              <span class="mode-link">进入新手教程 →</span>
            </div>
            <div class="mode-map" aria-hidden="true">
              <span>岗位</span>
              <span>Python</span>
              <span>Pytest</span>
              <span>HTTP</span>
              <span>接口</span>
              <span>断言</span>
              <span>项目</span>
              <span>表达</span>
            </div>
          </a>

          <a class="mode-secondary" href={interviewEntry.href}>
            <span class="mode-label">适合已有基础、准备面试的学习者</span>
            <h3>围绕项目、场景题和追问链集中冲刺</h3>
            <p>快速复盘高频知识，训练回答深度和项目表达。</p>
            <span class="mode-link">进入面试冲刺 →</span>
          </a>
        </div>
      </section>

      <section
        class="capability-section"
        id="capability-map"
        aria-labelledby="capability-title"
      >
        <div class="section-heading section-heading-with-link">
          <div>
            <h2 id="capability-title">四层能力地图</h2>
            <p>
              模块不是并列目录，而是从入门、技术、项目到面试表达的能力结构。
            </p>
          </div>
          <a class="section-link" href={allModulesHref}>从新手教程开始 →</a>
        </div>

        <div class="capability-map">
          <nav class="layer-nav" aria-label="能力层级">
            {
              capabilityLayers.map((layer, index) => (
                <a
                  href={`#layer-${index + 1}`}
                  class:list={{ "is-selected": index === 0 }}
                >
                  {layer.name}
                </a>
              ))
            }
          </nav>

          <div class="layer-groups">
            {
              capabilityLayers.map((layer, layerIndex) => (
                <section
                  class="layer-group"
                  id={`layer-${layerIndex + 1}`}
                  aria-labelledby={`layer-title-${layerIndex + 1}`}
                >
                  <h3 id={`layer-title-${layerIndex + 1}`}>{layer.name}</h3>
                  <div class="module-list">
                    {layer.modules.map((module, moduleIndex) => (
                      <a class="module-link" href={module.href}>
                        <span class="module-index" aria-hidden="true">
                          {String(
                            capabilityLayers
                              .slice(0, layerIndex)
                              .reduce(
                                (total, item) => total + item.modules.length,
                                0,
                              ) +
                              moduleIndex +
                              1,
                          ).padStart(2, "0")}
                        </span>
                        <strong>{module.title}</strong>
                        <span>{module.description}</span>
                      </a>
                    ))}
                  </div>
                </section>
              ))
            }
          </div>
        </div>
      </section>

      <section class="progress-summary" aria-labelledby="progress-title">
        <div>
          <p class="progress-kicker">学习复盘</p>
          <h2 id="progress-title">让每一步都能变成可表达的能力</h2>
          <p>
            完成课程后，回到项目、场景题和追问链中复用知识，而不是只记住一个概念。
          </p>
        </div>
        <div class="progress-result" aria-live="polite">
          <span class="progress-result-value" data-completed-count
            >尚未记录完成内容</span
          >
          <span class="progress-result-label"
            >完成一篇内容后，这里会提示下一步</span
          >
        </div>
      </section>
    </main>

    <script
      type="application/json"
      id="beginner-path-data"
      set:html={JSON.stringify(beginnerPath)}
    />

    <script>
      interface PathStep {
        slug: string;
        href: string;
        title: string;
        outcome: string;
        minutes: number;
      }

      interface ProgressData {
        completed?: number;
        completedSlugs?: string[];
      }

      function updatePathSlot(
        element: HTMLAnchorElement,
        step: PathStep,
        state: "complete" | "current" | "upcoming",
      ) {
        element.href = step.href;
        element.dataset.stepSlug = step.slug;
        element
          .closest(".path-item")
          ?.classList.toggle("is-complete", state === "complete");
        element
          .closest(".path-item")
          ?.classList.toggle("is-current", state === "current");

        if (state === "current") {
          element.setAttribute("aria-current", "step");
        } else {
          element.removeAttribute("aria-current");
        }

        const title = element.querySelector<HTMLElement>("[data-slot-title]");
        const outcome = element.querySelector<HTMLElement>(
          "[data-slot-outcome]",
        );
        const time = element.querySelector<HTMLElement>("[data-slot-time]");
        if (title) title.textContent = step.title;
        if (outcome) outcome.textContent = step.outcome;
        if (time) time.textContent = `${step.minutes} 分钟`;
      }

      function initializeProgress() {
        const dataElement = document.getElementById("beginner-path-data");
        if (!dataElement?.textContent) return;

        try {
          const path = JSON.parse(dataElement.textContent) as PathStep[];
          const rawProgress = localStorage.getItem("testdev:progress");
          const progress = rawProgress
            ? (JSON.parse(rawProgress) as ProgressData)
            : {};
          const completedSlugs = Array.isArray(progress.completedSlugs)
            ? progress.completedSlugs
            : [];
          const pathSlugs = new Set(path.map((step) => step.slug));
          const completedSet = new Set(
            completedSlugs.filter((slug) => pathSlugs.has(slug)),
          );
          const currentIndex = path.findIndex(
            (step) => !completedSet.has(step.slug),
          );
          const resolvedCurrentIndex =
            currentIndex === -1 ? path.length - 1 : currentIndex;
          const currentStep = path[resolvedCurrentIndex];
          const visibleStart = Math.min(
            Math.max(resolvedCurrentIndex - 1, 0),
            Math.max(path.length - 4, 0),
          );
          const visibleSteps = path.slice(visibleStart, visibleStart + 4);

          document
            .querySelectorAll<HTMLAnchorElement>("[data-path-slot]")
            .forEach((slot, index) => {
              const step = visibleSteps[index];
              if (!step) return;
              const state = completedSet.has(step.slug)
                ? "complete"
                : step.slug === currentStep.slug
                  ? "current"
                  : "upcoming";
              updatePathSlot(slot, step, state);
            });

          document
            .querySelectorAll<HTMLAnchorElement>("[data-continue-learning]")
            .forEach((link) => {
              link.href = currentStep.href;
            });

          const currentLink = document.querySelector<HTMLAnchorElement>(
            "[data-current-step-link]",
          );
          const currentOutcome = document.querySelector<HTMLElement>(
            "[data-current-step-outcome]",
          );
          const currentTime = document.querySelector<HTMLElement>(
            "[data-current-step-time]",
          );
          const progressTag = document.querySelector<HTMLElement>(
            "[data-path-progress]",
          );
          const completedCount = document.querySelector<HTMLElement>(
            "[data-completed-count]",
          );

          if (currentLink) {
            currentLink.href = currentStep.href;
            currentLink.textContent =
              currentIndex === -1
                ? "复盘新手路线并准备项目表达"
                : currentStep.title;
          }
          if (currentOutcome) {
            currentOutcome.textContent =
              currentIndex === -1
                ? "把已完成的练习整理成面试语言"
                : currentStep.outcome;
          }
          if (currentTime)
            currentTime.textContent = `${currentStep.minutes} 分钟`;
          if (progressTag) {
            progressTag.textContent =
              completedSet.size > 0
                ? `已完成 ${completedSet.size} / ${path.length}`
                : "从第 1 步开始";
          }
          if (completedCount) {
            completedCount.textContent =
              completedSet.size > 0
                ? `已完成 ${completedSet.size} 篇内容`
                : "尚未记录完成内容";
          }
        } catch {
          // 静态回退已包含完整的第一步入口，存储异常时保持页面可用。
        }
      }

      document.addEventListener("DOMContentLoaded", initializeProgress);
    </script>
  </body>
</html>

<style is:global>
  :root {
    --home-primary: #3b82f6;
    --home-primary-deep: #1d4ed8;
    --home-primary-darker: #173c87;
    --home-primary-soft: #eff6ff;
    --home-primary-mid: #bfdbfe;
    --home-accent: #b45309;
    --home-accent-soft: #fffbeb;
    --home-success: #059669;
    --home-success-soft: #d1fae5;
    --home-ink: #111827;
    --home-ink-soft: #4b5563;
    --home-ink-subtle: #6b7280;
    --home-bg: #ffffff;
    --home-surface: #f9fafb;
    --home-surface-muted: #f3f4f6;
    --home-border: #e5e7eb;
    --home-border-strong: #d1d5db;
    --home-radius-sm: 0.25rem;
    --home-radius-md: 0.5rem;
    --home-radius-lg: 0.75rem;
    --home-radius-xl: 1rem;
    --home-radius-full: 9999px;
    --home-shadow-sm:
      0 4px 6px -1px rgba(17, 24, 39, 0.08),
      0 2px 4px -2px rgba(17, 24, 39, 0.06);
    --home-shadow-button: 0 5px 10px rgba(37, 99, 235, 0.18);
    --home-ease: cubic-bezier(0.22, 1, 0.36, 1);
    --home-font:
      "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", system-ui,
      sans-serif;
  }

  * {
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    margin: 0;
    color: var(--home-ink);
    background: var(--home-bg);
    font-family: var(--home-font);
    font-kerning: normal;
  }

  a {
    color: inherit;
  }

  .site-header {
    position: sticky;
    z-index: 10;
    top: 0;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    min-height: 4rem;
    padding: 0.625rem clamp(1rem, 4vw, 3.25rem);
    background: rgba(255, 255, 255, 0.96);
    border-bottom: 1px solid var(--home-border);
  }

  .site-brand,
  .site-nav,
  .site-nav-link,
  .site-nav-action,
  .button,
  .path-panel-link,
  .section-link,
  .module-link,
  .mode-primary,
  .mode-secondary,
  .current-step-title {
    text-decoration: none;
  }

  .site-brand {
    display: inline-flex;
    align-items: center;
    gap: 0.625rem;
    min-height: 2.75rem;
    font-weight: 700;
    white-space: nowrap;
  }

  .site-brand-mark {
    display: grid;
    width: 1.75rem;
    height: 1.75rem;
    place-items: center;
    color: var(--home-bg);
    background: var(--home-primary-deep);
    border-radius: 0.4375rem;
    font-size: 0.8125rem;
  }

  .site-nav {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 0.25rem;
    flex-wrap: wrap;
  }

  .site-nav-link,
  .site-nav-action {
    display: inline-flex;
    align-items: center;
    min-height: 2.75rem;
    padding: 0 0.75rem;
    border-radius: var(--home-radius-md);
    font-size: 0.875rem;
    font-weight: 500;
  }

  .site-nav-link {
    color: var(--home-ink-soft);
  }

  .site-nav-link.is-active {
    color: var(--home-ink);
    font-weight: 650;
  }

  .site-nav-action {
    color: var(--home-primary-deep);
    background: var(--home-primary-soft);
    font-weight: 650;
  }

  .page-shell {
    width: min(100% - 2rem, 87.5rem);
    margin: 0 auto;
    padding: clamp(2.5rem, 6vw, 5rem) 0 4rem;
  }

  .hero-section {
    display: grid;
    grid-template-columns: minmax(0, 1.08fr) minmax(22rem, 0.92fr);
    gap: clamp(2.5rem, 6vw, 5.25rem);
    align-items: center;
    padding-bottom: clamp(3.5rem, 7vw, 6rem);
  }

  .hero-copy {
    max-width: 45rem;
  }

  .hero-signal,
  .path-panel-kicker,
  .progress-kicker {
    margin: 0;
    color: var(--home-primary-deep);
    font-size: 0.8125rem;
    font-weight: 650;
  }

  .hero-signal {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 1.125rem;
  }

  .hero-signal::before {
    width: 1.125rem;
    height: 0.125rem;
    background: var(--home-primary);
    content: "";
  }

  .hero-copy h1 {
    max-width: 11ch;
    margin: 0;
    font-size: clamp(2.5rem, 4.4vw, 4.25rem);
    line-height: 1.08;
    letter-spacing: -0.035em;
    text-wrap: balance;
  }

  .hero-description {
    max-width: 38rem;
    margin: 1.375rem 0 0;
    color: var(--home-ink-soft);
    font-size: 1.0625rem;
    line-height: 1.85;
    text-wrap: pretty;
  }

  .hero-actions {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
    margin-top: 1.75rem;
  }

  .button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    min-height: 2.75rem;
    padding: 0 1.0625rem;
    border-radius: 0.5625rem;
    font-size: 0.875rem;
    font-weight: 650;
    transition:
      background-color 200ms var(--home-ease),
      border-color 200ms var(--home-ease),
      transform 200ms var(--home-ease),
      box-shadow 200ms var(--home-ease);
  }

  .button-primary {
    color: var(--home-bg);
    background: var(--home-primary-deep);
    box-shadow: var(--home-shadow-button);
  }

  .button-secondary {
    color: var(--home-ink);
    background: var(--home-bg);
    border: 1px solid var(--home-border-strong);
  }

  .path-panel {
    position: relative;
    overflow: hidden;
    padding: 1.5rem;
    background: var(--home-surface);
    border: 1px solid var(--home-border);
    border-radius: var(--home-radius-lg);
  }

  .path-panel::before {
    position: absolute;
    inset: 0 0 auto;
    height: 0.25rem;
    background: var(--home-primary-deep);
    content: "";
  }

  .path-panel-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1rem;
  }

  .path-panel-header h2 {
    max-width: 22rem;
    margin: 0.3125rem 0 0;
    font-size: 1.125rem;
    line-height: 1.4;
    text-wrap: balance;
  }

  .status-tag {
    flex: 0 0 auto;
    padding: 0.25rem 0.5rem;
    color: var(--home-success);
    background: var(--home-success-soft);
    border-radius: var(--home-radius-full);
    font-size: 0.75rem;
    font-weight: 650;
  }

  .current-step {
    display: grid;
    gap: 0.25rem;
    margin-top: 1.25rem;
    padding: 1rem;
    background: var(--home-bg);
    border: 1px solid var(--home-primary-mid);
    border-radius: var(--home-radius-md);
  }

  .current-step-label {
    color: var(--home-primary-deep);
    font-size: 0.75rem;
    font-weight: 650;
  }

  .current-step-title {
    width: fit-content;
    color: var(--home-ink);
    font-size: 0.9375rem;
    font-weight: 700;
  }

  .current-step p {
    margin: 0;
    color: var(--home-ink-soft);
    font-size: 0.8125rem;
    line-height: 1.6;
  }

  .current-step-time {
    color: var(--home-ink-subtle);
    font-size: 0.75rem;
  }

  .path-list {
    position: relative;
    display: grid;
    gap: 0.25rem;
    margin: 1rem 0 0;
    padding: 0;
    list-style: none;
  }

  .path-list::before {
    position: absolute;
    top: 1.375rem;
    bottom: 1.375rem;
    left: 0.625rem;
    width: 1px;
    background: var(--home-border-strong);
    content: "";
  }

  .path-item {
    position: relative;
  }

  .path-item > a {
    display: grid;
    grid-template-columns: 1.25rem minmax(0, 1fr) auto;
    gap: 0.625rem;
    align-items: center;
    min-height: 3.25rem;
    padding: 0.375rem 0;
    text-decoration: none;
  }

  .path-marker {
    z-index: 1;
    width: 1.25rem;
    height: 1.25rem;
    background: var(--home-bg);
    border: 2px solid var(--home-border-strong);
    border-radius: var(--home-radius-full);
  }

  .path-item.is-current .path-marker {
    background: var(--home-primary-deep);
    border-color: var(--home-primary-deep);
    box-shadow: 0 0 0 4px var(--home-primary-soft);
  }

  .path-item.is-complete .path-marker {
    background: var(--home-success);
    border-color: var(--home-success);
    box-shadow: inset 0 0 0 4px var(--home-success-soft);
  }

  .path-item-copy {
    display: grid;
    gap: 0.125rem;
    min-width: 0;
  }

  .path-item-copy strong {
    overflow: hidden;
    color: var(--home-ink);
    font-size: 0.8125rem;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .path-item-copy span,
  .path-item-time {
    color: var(--home-ink-subtle);
    font-size: 0.75rem;
  }

  .path-item-time {
    white-space: nowrap;
  }

  .path-panel-link,
  .section-link {
    display: inline-flex;
    align-items: center;
    min-height: 2.75rem;
    color: var(--home-primary-deep);
    font-size: 0.8125rem;
    font-weight: 650;
  }

  .path-panel-link {
    margin-top: 0.5rem;
  }

  .decision-section,
  .capability-section {
    padding: clamp(3rem, 7vw, 5.5rem) 0 0;
    border-top: 1px solid var(--home-border);
  }

  .section-heading {
    margin-bottom: 1.375rem;
  }

  .section-heading-with-link {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 1.5rem;
  }

  .section-heading h2,
  .progress-summary h2 {
    margin: 0;
    font-size: clamp(1.5rem, 2.4vw, 2rem);
    line-height: 1.25;
    letter-spacing: -0.02em;
    text-wrap: balance;
  }

  .section-heading p,
  .progress-summary p {
    max-width: 38rem;
    margin: 0.4375rem 0 0;
    color: var(--home-ink-soft);
    font-size: 0.875rem;
    line-height: 1.75;
  }

  .mode-layout {
    display: grid;
    grid-template-columns: minmax(0, 1.25fr) minmax(17rem, 0.75fr);
    gap: 1.125rem;
  }

  .mode-primary,
  .mode-secondary {
    min-height: 11rem;
    border-radius: var(--home-radius-lg);
    transition:
      transform 200ms var(--home-ease),
      box-shadow 200ms var(--home-ease),
      border-color 200ms var(--home-ease);
  }

  .mode-primary {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 11.25rem;
    gap: 1.5rem;
    padding: 1.5rem;
    color: var(--home-bg);
    background: var(--home-primary-darker);
  }

  .mode-secondary {
    display: block;
    padding: 1.5rem;
    background: var(--home-accent-soft);
    border: 1px solid #fde68a;
  }

  .mode-label {
    display: block;
    margin-bottom: 0.6875rem;
    color: rgba(255, 255, 255, 0.74);
    font-size: 0.75rem;
    font-weight: 500;
  }

  .mode-secondary .mode-label {
    color: var(--home-accent);
  }

  .mode-primary h3,
  .mode-secondary h3 {
    max-width: 22rem;
    margin: 0;
    font-size: 1.3125rem;
    line-height: 1.4;
    text-wrap: balance;
  }

  .mode-primary p,
  .mode-secondary p {
    max-width: 27rem;
    margin: 0.625rem 0 1rem;
    font-size: 0.8125rem;
    line-height: 1.7;
  }

  .mode-primary p {
    color: rgba(255, 255, 255, 0.76);
  }

  .mode-secondary p {
    color: #785a2d;
  }

  .mode-link {
    font-size: 0.8125rem;
    font-weight: 650;
  }

  .mode-map {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    align-content: center;
    gap: 0.5rem;
  }

  .mode-map span {
    display: grid;
    aspect-ratio: 1;
    place-items: center;
    color: rgba(255, 255, 255, 0.82);
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.16);
    border-radius: var(--home-radius-md);
    font-size: 0.6875rem;
  }

  .capability-map {
    display: grid;
    grid-template-columns: 11.25rem minmax(0, 1fr);
    gap: 1.875rem;
  }

  .layer-nav {
    position: sticky;
    top: 5rem;
    display: grid;
    align-self: start;
    gap: 0.25rem;
  }

  .layer-nav a {
    display: flex;
    align-items: center;
    min-height: 2.75rem;
    padding: 0 0.75rem;
    color: var(--home-ink-soft);
    border-radius: var(--home-radius-md);
    font-size: 0.8125rem;
    text-decoration: none;
  }

  .layer-nav a.is-selected {
    color: var(--home-primary-deep);
    background: var(--home-primary-soft);
    font-weight: 650;
  }

  .layer-groups {
    border-top: 1px solid var(--home-border);
  }

  .layer-group {
    scroll-margin-top: 5.5rem;
  }

  .layer-group h3 {
    margin: 0;
    padding: 1.125rem 0 0.625rem;
    color: var(--home-ink-soft);
    font-size: 0.8125rem;
    font-weight: 650;
  }

  .module-list {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    border-top: 1px solid var(--home-border);
  }

  .module-link {
    display: grid;
    align-content: start;
    min-height: 8.25rem;
    padding: 1.125rem;
    border-right: 1px solid var(--home-border);
    border-bottom: 1px solid var(--home-border);
    transition:
      background-color 200ms var(--home-ease),
      color 200ms var(--home-ease);
  }

  .module-link:nth-child(3n) {
    border-right: 0;
  }

  .module-index {
    margin-bottom: 1rem;
    color: #8a99ad;
    font-size: 0.6875rem;
    font-variant-numeric: tabular-nums;
  }

  .module-link strong {
    margin-bottom: 0.375rem;
    font-size: 0.9375rem;
  }

  .module-link > span:last-child {
    color: var(--home-ink-soft);
    font-size: 0.75rem;
    line-height: 1.65;
  }

  .progress-summary {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 2rem;
    align-items: center;
    margin-top: clamp(3.5rem, 8vw, 6.5rem);
    padding: 1.5rem 0;
    border-top: 1px solid var(--home-border);
    border-bottom: 1px solid var(--home-border);
  }

  .progress-result {
    display: grid;
    gap: 0.25rem;
    min-width: 13rem;
    padding-left: 1.5rem;
    border-left: 1px solid var(--home-border);
  }

  .progress-result-value {
    font-size: 1rem;
    font-weight: 700;
  }

  .progress-result-label {
    color: var(--home-ink-subtle);
    font-size: 0.75rem;
  }

  .site-brand:focus-visible,
  .site-nav-link:focus-visible,
  .site-nav-action:focus-visible,
  .button:focus-visible,
  .path-item a:focus-visible,
  .path-panel-link:focus-visible,
  .current-step-title:focus-visible,
  .mode-primary:focus-visible,
  .mode-secondary:focus-visible,
  .layer-nav a:focus-visible,
  .module-link:focus-visible,
  .section-link:focus-visible {
    outline: 3px solid var(--home-primary-mid);
    outline-offset: 3px;
  }

  @media (hover: hover) {
    .site-nav-link:hover {
      color: var(--home-ink);
      background: var(--home-surface);
    }

    .site-nav-action:hover {
      background: #dbeafe;
    }

    .button-primary:hover {
      background: var(--home-primary-darker);
      box-shadow: var(--home-shadow-sm);
      transform: translateY(-1px);
    }

    .button-secondary:hover {
      border-color: var(--home-primary-mid);
      transform: translateY(-1px);
    }

    .mode-primary:hover,
    .mode-secondary:hover {
      transform: translateY(-2px);
      box-shadow: var(--home-shadow-sm);
    }

    .mode-secondary:hover {
      border-color: #fcd34d;
    }

    .module-link:hover {
      color: var(--home-primary-deep);
      background: var(--home-surface);
    }

    .path-item a:hover strong,
    .path-panel-link:hover,
    .current-step-title:hover,
    .section-link:hover {
      color: var(--home-primary-deep);
    }
  }

  @media (max-width: 68rem) {
    .site-header {
      align-items: flex-start;
      flex-direction: column;
      gap: 0.25rem;
      padding-top: 0.5rem;
      padding-bottom: 0.5rem;
    }

    .site-nav {
      justify-content: flex-start;
    }

    .hero-section {
      grid-template-columns: 1fr;
    }

    .path-panel {
      max-width: 43rem;
    }
  }

  @media (max-width: 52rem) {
    .mode-layout,
    .capability-map,
    .progress-summary {
      grid-template-columns: 1fr;
    }

    .layer-nav {
      position: static;
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .module-list {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .module-link:nth-child(3n) {
      border-right: 1px solid var(--home-border);
    }

    .module-link:nth-child(2n) {
      border-right: 0;
    }

    .progress-result {
      padding: 1rem 0 0;
      border-top: 1px solid var(--home-border);
      border-left: 0;
    }
  }

  @media (max-width: 40rem) {
    .page-shell {
      width: min(100% - 1.25rem, 87.5rem);
      padding-top: 2.25rem;
    }

    .site-header {
      position: static;
      padding-right: 0.625rem;
      padding-left: 0.625rem;
    }

    .site-nav {
      width: 100%;
      gap: 0;
    }

    .site-nav-link,
    .site-nav-action {
      padding: 0 0.625rem;
      font-size: 0.8125rem;
    }

    .hero-section {
      gap: 2rem;
      padding-bottom: 3rem;
    }

    .hero-copy h1 {
      max-width: none;
      font-size: clamp(2.25rem, 12vw, 3rem);
      letter-spacing: -0.03em;
    }

    .hero-description {
      font-size: 1rem;
      line-height: 1.75;
    }

    .hero-actions,
    .hero-actions .button {
      width: 100%;
    }

    .path-panel {
      padding: 1.25rem;
    }

    .path-panel-header,
    .section-heading-with-link {
      align-items: flex-start;
      flex-direction: column;
      gap: 0.5rem;
    }

    .mode-primary {
      grid-template-columns: 1fr;
    }

    .mode-map {
      display: none;
    }

    .layer-nav,
    .module-list {
      grid-template-columns: 1fr;
    }

    .module-link,
    .module-link:nth-child(2n),
    .module-link:nth-child(3n) {
      border-right: 0;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    html {
      scroll-behavior: auto;
    }

    *,
    *::before,
    *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }
  }
</style>
